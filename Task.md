# JavDBMagnet MVP：RD 完成時間與四階段分頁

## 背景

目前「送至 Real-Debrid 進度」中的項目如果先顯示為「RD 處理中」，稍後按下「全部重試（查 RD）」並確認已完成，該列只會在原位置改成「已完成」。使用者無法快速辨認哪些項目是剛剛才完成的，也看不到應用程式在什麼時間確認它已完成。

目前主要流程也集中在同一個長頁面。MVP 要把工作流程拆成四個明確分頁：

1. 擷取 Magnet
2. 挑選 Magnet
3. 送出 RD 並取得連結
4. 設定

## MVP 目標

- 每個 RD 已完成項目顯示「完成時間」。
- 後來完成的項目排在較下面，讓新完成項目容易辨識。
- 將主要操作拆成四個分頁，切換分頁時不遺失目前工作狀態。
- 不改變現有的 JavDB、Magnet、Real-Debrid 核心處理流程。

## 現況盤點

目前原始碼已有可沿用的基礎：

- `App.svelte` 已有 `activeTab`，值為 `search | select | rd | settings`。
- `RdSendProgress` 已有可選的 `completed_at`。
- 首次送出即完成，以及 pending 重試後轉為完成時，已會寫入 ISO-8601 時間。
- 已有 `sortCompletedRowsByCompletionTime()`，但目前主要用於複製下載連結，RD 表格仍直接迭代 `rdSendProgress`。
- RD 表格尚未顯示完成時間。
- 部分設定功能仍位於 RD 分頁，需要依本文件重新歸位。

因此本工作以補齊 UI、排序、分頁內容歸屬與測試為主，不重做既有 RD 流程。

---

## 功能一：RD 完成時間與排序

### 1.1 完成時間的定義

`completed_at` 代表「JavDBMagnet 首次確認該項目已完成的時間」，不是 Real-Debrid 伺服器實際完成下載的精確時間。

原因是 pending 項目只有在使用者按下「全部重試（查 RD）」時，應用程式才會得知它已完成。畫面文案可顯示「完成時間」，並以 tooltip 或說明文字註明這是「本程式確認完成的時間」。

### 1.2 寫入規則

- `rd_send_magnet` 第一次回傳 `completed` 時，寫入目前時間。
- `rd_check_pending` 經由「全部重試（查 RD）」第一次回傳 `completed` 時，寫入目前時間。
- 時間儲存格式為 UTC ISO-8601，例如 `2026-07-26T13:45:12.345Z`。
- 同一列已經有 `completed_at` 時，不可因重新渲染、複製連結或再次查詢而覆寫。
- 單次重試批次中，每一列以實際收到 completed 結果的時間為準。

### 1.3 畫面顯示

在「3. RD 下載連結」的進度表新增「完成時間」欄：

- 已完成：顯示本機時區時間，格式建議為 `YYYY-MM-DD HH:mm:ss`。
- 尚未完成或錯誤：顯示 `—`。
- 完整 ISO 時間可放在 `title`，方便需要時查閱。
- 欄名或輔助說明需讓使用者知道這是應用程式確認完成的時間。

### 1.4 排序規則

建立衍生資料 `rdDisplayRows`，不要直接改動 `rdSendProgress` 原始陣列：

1. 尚未完成的列顯示在上方，包含 `pending`、`sending`、`in_pending`、`error`。
2. 尚未完成列保留原本相對順序。
3. 已完成列集中顯示在下方。
4. 已完成列依 `completed_at` 由早到晚排列。
5. 最新完成的列位於最下方。
6. 相同時間使用原始順序作為穩定排序依據。
7. 舊資料若沒有 `completed_at`，顯示 `—`，並排在有時間的已完成列之前。

此排序只影響畫面顯示，不得改變：

- 批次送出順序。
- `handle_id` 與 `torrent_id` 對應。
- pending 重試比對。
- 下載連結的內容。

### 1.5 建議的視覺提示

MVP 必做為「時間欄＋排序」。若成本很低，可額外讓剛從「RD 處理中」轉為「已完成」的列短暫高亮 1–2 秒，但不可取代時間欄。

### 1.6 MVP 持久化範圍

MVP 的完成時間只需涵蓋本次應用程式執行期間的 RD 進度列。關閉程式後保存完整已完成歷史、跨裝置同步、查詢 Real-Debrid 真實完成時間，均不在本次範圍。

---

## 功能二：四個工作流程分頁

### 2.1 共通行為

- 頁面頂部固定提供四個清楚可辨識的分頁按鈕。
- 預設開啟「1. 擷取 Magnet」。
- 切換分頁只改變顯示內容，不清除已輸入 URL、擷取結果、勾選狀態、RD 進度、pending 清單或設定草稿。
- 執行中的擷取或 RD 工作不因切換分頁而中止。
- 分頁按鈕需有 active 狀態，並使用正確的 `aria` 屬性。
- 視窗較窄時允許分頁換行或水平捲動，不得讓文字與按鈕重疊。

### 2.2 分頁 1：擷取 Magnet

放置：

- JavDB 網址批次輸入。
- 開始擷取、取消擷取與擷取進度。
- 每個網址的成功／失敗結果摘要。
- 手動貼上 `magnet:` 並加入清單的功能。
- 「前往挑選 Magnet」按鈕。

不放置：

- 完整 Magnet 勾選表格。
- RD 送出進度與下載連結。
- Token、Cookies、主題等設定。

### 2.3 分頁 2：挑選 Magnet

放置：

- 從 JavDB 擷取或手動貼上的 Magnet 清單。
- 每列勾選框。
- 全選、全不選、篩選、排序及群組展開／收合。
- 已選數量／總數。
- 複製已選 Magnet。
- 「送至 RD」或「前往 RD」主要操作。

空狀態需引導使用者回到「1. 擷取 Magnet」。

### 2.4 分頁 3：送出 RD 並取得連結

分頁名稱在畫面上使用簡短且明確的文字，例如「3. RD 下載連結」。

放置：

- 已選 Magnet 數量與送出按鈕。
- 批次送出進度。
- 完成／待處理／錯誤／下載連結數量摘要。
- 含「完成時間」的新 RD 進度表。
- 單列複製、單連結複製、全部複製。
- pending 清單。
- 「全部重試（查 RD）」、「重讀本機紀錄」、「全部清除」及取消重試。

不放置：

- RD Token 編輯。
- Cookies 編輯。
- 主題、縮放、Sidecar 診斷、資料路徑或舊資料匯入。

若 Token 尚未設定，送出按鈕應停用並提供可前往「4. 設定」的明確提示。

### 2.5 分頁 4：設定

集中放置所有設定與維護功能：

- Real-Debrid Token 的新增、更換、驗證與清除。
- JavDB Cookies 的新增、更換、驗證與清除。
- RD 選檔策略、最小檔案大小與 cache wait。
- 主題與 UI 縮放。
- 資料目錄、Log 目錄及開啟目錄。
- Sidecar Ping／診斷。
- 舊版設定與 pending 資料匯入。

設定儲存成功後，不清除其他三個分頁中的工作資料。

### 2.6 分頁摘要

分頁可顯示精簡摘要，方便不切頁也能知道狀態：

- 擷取 Magnet：成功數／總數。
- 挑選 Magnet：已選數／可選總數。
- RD 下載連結：已完成數／本批總數；尚無批次時顯示 pending 數。
- 設定：可顯示 Token／Cookies 是否已設定，避免顯示任何敏感值。

---

## 實作工作清單

### A. RD 完成時間

- [ ] 在 `App.svelte` 建立只供畫面使用的 `rdDisplayRows`。
- [ ] 套用「未完成在上、完成在下、最新完成置底」的穩定排序。
- [ ] RD 進度表改為迭代 `rdDisplayRows`。
- [ ] 新增「完成時間」欄與本機時間格式化函式。
- [ ] 確認首次送出完成會設定 `completed_at`。
- [ ] 確認 pending 重試轉完成會設定 `completed_at`。
- [ ] 確認既有 `completed_at` 不會被覆寫。
- [ ] 確認舊列缺少時間時不會造成排序或渲染錯誤。

### B. 四分頁

- [ ] 保留並整理現有 `activeTab` 導覽。
- [ ] 將每個 section 移至本文件指定的分頁。
- [ ] 將 RD Token 管理從 RD 下載頁移至設定頁。
- [ ] 確認分頁切換不會重設任何工作狀態。
- [ ] 加入空狀態與跨分頁導引按鈕。
- [ ] 補齊分頁的 active、鍵盤操作、ARIA 與窄視窗樣式。

### C. 測試

- [ ] `rdSender.test.ts`：首次完成會產生 `completed_at`。
- [ ] `rdSender.test.ts`：pending 重試轉完成會產生 `completed_at`。
- [ ] `rdSender.test.ts`：已有完成時間不會被非完成事件覆寫。
- [ ] 排序測試：未完成列在上且保持原順序。
- [ ] 排序測試：已完成列由早到晚，最新完成在最下方。
- [ ] 排序測試：相同時間維持穩定順序。
- [ ] 排序測試：缺少 `completed_at` 的舊列安全顯示並排在有時間列之前。
- [ ] 元件測試：四個分頁只顯示各自負責的內容。
- [ ] 元件測試：切換分頁後輸入、選取與 RD 進度仍保留。
- [ ] 手動測試：pending 項目經「全部重試（查 RD）」轉完成後，立即出現在完成區最下方並顯示新時間。

## 驗收情境

### 情境 1：稍後完成的 pending 項目

1. 送出 3 個 Magnet，其中 2 個完成、1 個顯示「RD 處理中」。
2. 等待數分鐘。
3. 按下「全部重試（查 RD）」。
4. RD 回傳該 pending 項目已完成。

預期：

- 狀態立即改成「已完成」。
- 列中出現本機格式的完成時間。
- 該列移至完成區最下方。
- 原有兩筆完成列仍按較早時間排列。
- 下載連結可正常複製。

### 情境 2：仍在處理

1. 對 pending 項目執行「全部重試（查 RD）」。
2. RD 仍回傳 pending。

預期：

- 狀態維持「RD 處理中」。
- 完成時間顯示 `—`。
- 該列留在未完成區，不移入完成區。

### 情境 3：分頁切換

1. 在擷取頁輸入並擷取 JavDB 網址。
2. 到挑選頁勾選 Magnet。
3. 到 RD 頁開始送出。
4. 送出期間切到設定頁，再切回 RD 頁。

預期：

- 擷取結果與勾選狀態保留。
- RD 工作不中斷。
- 回到 RD 頁可看到最新進度。
- 設定頁不顯示 RD 下載表，RD 頁不顯示 Token／Cookies 編輯器。

## 完成定義

- 上述兩項 MVP 均可在 Windows Tauri GUI 中操作。
- 所有新增與既有前端測試通過。
- `npm run check` 通過。
- Rust／Sidecar 通訊格式不因本次 UI 調整而破壞。
- 以實際 pending → retry → completed 流程完成一次手動驗收。
- 產出新的 portable EXE 後，確認四分頁、完成時間與排序均存在於實際執行檔中。

## 非本次範圍

- 跨重啟保存完整已完成歷史。
- 查詢或還原 Real-Debrid 伺服器的精確完成時刻。
- 自訂排序方向或拖曳排序。
- 分頁路由、瀏覽器網址或返回／前進歷史。
- 大幅重設視覺風格。
- 修改 Real-Debrid API、選檔策略或下載邏輯。
