{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "^(Bash|PowerShell)$",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -X utf8 \"$(git rev-parse --show-toplevel)/.agent-hooks/recursive_delete_guard.py\" --runner codex",
            "commandWindows": "powershell.exe -NoProfile -Command \"$root = git rev-parse --show-toplevel; python -X utf8 (Join-Path $root '.agent-hooks\\recursive_delete_guard.py') --runner codex\"",
            "timeout": 10,
            "statusMessage": "Checking recursive deletion policy"
          }
        ]
      }
    ]
  }
}
