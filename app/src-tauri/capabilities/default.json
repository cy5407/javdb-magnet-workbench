{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window. M2 only exposes core IPC; plugin permissions added in later milestones as features need them.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    {
      "identifier": "shell:allow-open",
      "allow": [
        { "url": "https://real-debrid.com/apitoken" }
      ]
    }
  ]
}
